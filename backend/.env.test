# .env

GOOGLE_CLOUD_PROJECT_ID=Your-GCP-Project-ID
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/callback/google"

GCP_LOCATION=global
GCP_REGION=global
IS_SERVICE_ACCOUNT=false

# Steward UpdateEntry kill-switch (set true to enable write routes)
ENABLE_ENTRY_WRITES=false

PORT=3000
