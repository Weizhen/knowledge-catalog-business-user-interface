/** Product name shown next to the Ericsson logo in the app chrome. */
export const APP_PRODUCT_NAME =
  'Group Marketing Data Context Management Platform';

/** Short lines for the navbar lockup (fits beside the logo). */
export const APP_PRODUCT_NAME_LINE1 = 'Group Marketing Data Context';
export const APP_PRODUCT_NAME_LINE2 = 'Management Platform';

/**
 * Build version for support / Cloud Run debugging.
 * Keep in sync with root package.json "version".
 */
export const APP_VERSION = '1.4.4';
